#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
	int n = 0;
	float p=0.15, j=(float) n;
	int x=0, i=round(p*100.0), l = 0;
	{
		printf("Insert value is 15%% of x");
		printf("\nEnter value: ");
		scanf("%d", &n);
		printf("%.2f x %d", p, n);
		printf("\n%.2f/%.2f*x %d/%.2f", p, p, n, p);
		printf("\nx = %d*x/%.2f", n, p);
		printf("\n%d/%.2f", n, p);
		printf("\n%d/%d", n, i);
		j=round(n*100.0);
		*/printf("\n%0.f", j);
		printf("\n%d", i);*/
		l = j/i;
		printf("\n%d", l);		
	}
	return 0;
}
