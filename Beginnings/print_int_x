int main()
{
    int x = 2;
    printf("%d", x);
}

V2

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x = 2;
    if (x == 2) {
        printf("X is equal to %d", x);
    }
    else if (x < 2) {
        printf("X is less than %d", x);
    }
    else {
        printf("X is greater than %d", x);
    }
}

V3

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x;
    printf("Enter the value of x: ");
    scanf("%d", &x);

    if (x == 2) {
        printf("X is equal to %d", x);
    }
    else if (x < 2) {
        printf("X is less than %d", x);
    }
    else {
        printf("X is greater than %d", x);
    }
}
